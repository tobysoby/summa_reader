{
  "name": "test_function",
  "description": "This is the default fulfillment for a Dialogflow agents using Cloud Functions for Firebase",
  "version": "0.0.1",
  "private": true,
  "license": "Apache Version 2.0",
  "author": "Google Inc.",
  "engines": {
    "node": "~6.0"
  },
  "scripts": {
    "start": "firebase serve --only functions:test_function",
    "deploy": "firebase deploy --only functions:test_function"
  },
  "dependencies": {
    "actions-on-google": "^1.5.x",
    "firebase-admin": "~5.4.0",
    "firebase-functions": "^0.7.0",
    "apiai": "^4.0.3"
  }
}
